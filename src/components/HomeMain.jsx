import Container from 'react-bootstrap/Container';
import { Form } from './Form';

export const HomeMain = (props) => {
    return (
        <Container>
            <Form />
        </Container>
    );
};
